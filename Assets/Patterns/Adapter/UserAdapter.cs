/*
using UnityEngine;

public class UserAdapter : ScriptableObject
{
    private User m_User;

    private void Awake()
    {
        m_User = User.CreateInstance<User>();
        Debug.Log(m_User.GetFirstName(77));
    }

    public string GetFullName(User user, int userId)
    {
        return user.GetFirstName(userId) + " " + user.GetLastName(userId);
    }
}
*/
