using UnityEngine;
/*
public class ClientAdapter : MonoBehaviour
{
    private User m_User;
    private UserAdapter m_UserAdapter;

    void Update()
    {
        // Press U to get the user info.
        if (Input.GetKeyDown(KeyCode.U))
        {
            m_User = User.CreateInstance<User>();
            m_UserAdapter = UserAdapter.CreateInstance<UserAdapter>();

            string FirstName = m_User.GetFirstName(79);
            string LastName = m_User.GetLastName(79);
            string FullName_Formal = m_User.GetFullName(79);

            string FullName_Normal = m_UserAdapter.GetFullName(m_User, 79);

            Debug.Log(FirstName);
            Debug.Log(LastName);

            Debug.Log(FullName_Formal);
            Debug.Log(FullName_Normal);
        }
    }

}
*/
